---
# Service options.
docker_service_state: started
docker_service_enabled: true
docker_restart_handler_state: restarted
docker_ufw_restart_handler_state: restarted

# Used only for Debian/Ubuntu. Switch 'stable' to 'edge' if needed.
docker_archs:
  x86_64: amd64
  amd64: amd64
  aarch64: arm64
  arm64: arm64
docker_apt_release_channel: stable
docker_apt_arch: amd64
docker_docker_apt_repository: 'deb [arch={{ docker_archs[ansible_facts.architecture] }} signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/{{ ansible_facts["distribution"]|lower }} {{ ansible_facts["distribution_release"] }} {{ docker_apt_release_channel }}'
docker_apt_ignore_key_error: true

# Used only for RedHat/CentOS/Fedora.
docker_yum_repo_url: https://download.docker.com/linux/{{ (ansible_distribution == "Fedora") | ternary("fedora","centos") }}/docker-{{ docker_edition }}.repo
docker_yum_repo_enable_edge: 0
docker_yum_repo_enable_test: 0

docker_version: "5:29.3.0-1~ubuntu.24.04~noble"
docker_compose_version: "5.1.0-1~ubuntu.24.04~noble"